// Compatibility re-export: the parser is now production-source code so the
// Phase 11 browser prototype can reuse the exact Phase 3/4 offline parser.
export * from '../../src/data-foundation/dokkaninfo-saved-stage.mjs';
